import React from 'react';

import { Button } from 'components';

import './index.scss';

const App = () => {
  return (
    <main className="App">
      <Button>
        Login
      </Button>
    </main>
  )
};

export default App;
